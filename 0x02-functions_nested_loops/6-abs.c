#include "main.h"
/**
 * _abs - Computes the absolute value
 *Description: i for an integer.
 *
 *  @c:The number to be computed.
 *
 *  Return: absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
